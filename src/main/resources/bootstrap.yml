spring:
  application:
    name: account-service
  cloud:
    config:
      discovery:
        enabled: true
      username: config-server
      password: config-server
      fail-fast: true
encrypt:
  key: ABCDEFGHIJKLMNOPQRSTUVWXYZ